import React from 'react';
import PropTypes from 'prop-types';

const Scrapping = props => {
    console.log("tessssssst")
    return (
        <div>
            hello
        </div>
    );
};

Scrapping.propTypes = {
    
};

export default Scrapping;